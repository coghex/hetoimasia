# #265's Vulkan groups' Linux evidence

**Pending.** This document will retain the Linux receipts for issue #265 — the
`test.vulkan-headless` and `test.vulkan-native` receipts, and the native
group's per-scenario records, that the pull request's validation run writes on
the `vulkan` worker. Until they are copied here, Linux verification of this
change is pending, not passed. The macOS evidence is
[`macos-recording-split.md`](macos-recording-split.md).
