# 2D rendering — planned

Reserved component; no implementation exists yet.

Will supply sprite batching, painter ordering, clipping, pixel snapping, and
text composition over the shared GPU infrastructure. Game-specific isometric
projection, face-map semantics, and Synarchy compatibility belong in an adapter.
A small 2D consumer should arrive early alongside the first 3D scene.
