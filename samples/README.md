# Samples

The first console consumer currently lives in root `app/`. Add independent
rendering examples here once their components exist: a minimal 3D scene, then
a small 2D scene using the same infrastructure. Samples use public interfaces.
They are architecture checks and runnable examples, not engine dependencies.
